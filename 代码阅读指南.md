# NL2SQL-Agent（自然语言转 SQL Agent）代码阅读指南

本工程比一般的 CRUD 项目大：后端 Python 约 3000 行（60 余个文件）、前端 React 约 1000 行，且包含「元数据知识库构建」和「自然语言数据操作」两条主线。建议按「入口 → 数据流」的顺序读，先读在线数据操作链路，再补离线构建链路。

## 主干数据流

```
① 离线构建（先跑一次，产生知识库）
build_meta_knowledge.py → MetaKnowledgeService → MySQL(结构化) / Qdrant(字段·指标向量) / ES(字段取值)

② 在线数据操作（核心链路）
main.py → query_router → QueryService → graph.astream → LangGraph 节点链 → SSE → React
```

② 中的 LangGraph 节点链是整条主线的骨架：

```
extract_keywords
  → [ recall_column | recall_value | recall_metric ]   # 三路并行召回
  → merge_retrieved_info
  → [ filter_table | filter_metric ]                     # 两条并行过滤
  → add_extra_context → generate_sql → validate_sql
  → (条件边) correct_sql / classify_sql_type            # 校验失败先修正，再识别 SQL 类型
  → SELECT：run_sql → END
  → 写操作：resolve_insert_primary_key(INSERT主键预检) → estimate_impact(影响预估+快照)
      → wait_for_human_input(interrupt 人工审批)
      → 确认：run_sql → END（执行成功后审计落库，支撑 Time-Travel 回滚）
      → 取消：cancel_write → END
```

## 推荐阅读顺序

### 第 0 步：先建立整体认识

- 读 `README.md` 的「系统架构」表格，理解两条主线分别做什么。
- 读 `conf/app_config.yaml` —— 它定义了要连接哪些外部服务（MySQL×2 / Qdrant / Embedding / ES / LLM），`app/conf/app_config.py:108` 用 `load_dotenv` 加载 `.env` 后解析 `${oc.env:...}` 占位符。

### 第 1 步：`main.py` 与 API 层（真正入口）

- `main.py:18` 创建 FastAPI 应用并把 `lifespan` 交给框架管理；`main.py:24` 的中间件 `add_request_id` 用 `ContextVar` 为每个请求生成 `request_id`（是日志追踪的根基）。当前注册了 4 组路由：`query_router`（数据操作）、`session_router`（会话历史）、`human_feedback_router`（写操作审批续流）、`rollback_router`（审计查询与回滚）。
- 往下跳 `app/api/lifespan.py:27` —— 启动阶段初始化 5 个外部客户端，这就是「Client Manager 单例」的初始化点。
- `app/api/routers/query_router.py:22` 定义 `POST /api/query`，只声明请求体和依赖，具体组装收敛在 `app/api/dependencies.py:85` 的 `get_query_service`——一路 `Depends` 递归组装出全部仓储和客户端，路由本身零基础设施感知。

### 第 2 步：`app/agent/graph.py`（流程编排核心，最重要）

- `graph.py:52-69` 注册节点：三路召回 / 过滤 / 上下文 / SQL 闭环节点，以及写操作审批链路的 6 个节点（`classify_sql_type` / `resolve_insert_primary_key` / `estimate_impact` / `wait_for_human_input` / `cancel_write` / `run_sql`）。
- 重点看两条条件边：① `validate_sql` 按 `state["error"]` 决定走 `run_sql` 还是 `correct_sql`；② `classify_sql_type` 按 `sql_type` 分流——SELECT 直接执行，写操作进入「主键预检 → 影响预估 → 人工审批」链路；`wait_for_human_input` 用 `interrupt()` 挂起，审批结果（approve / reject）决定执行还是取消。
- 同时读 `state.py:63`（`DataAgentState`，节点间共享的可合并数据）和 `context.py:22`（`DataAgentContext`，不可合并的外部依赖）。理解「State 放业务数据、Context 放工具对象」的分离是本项目读懂的关键。

### 第 3 步：`QueryService` → 节点（数据如何流转）

- `app/services/query_service.py:45` 把用户问题装进 `State`、组装好 `Context`，用 `graph.astream(..., stream_mode="custom")`（`query_service.py:61`）消费节点写出的进度，并逐段包装成 SSE 文本返回。
- 然后任选几个节点细读，它们共享同一套范式：从 `state` 读数据 → 从 `runtime.context` 取工具 → 用 `writer(...)` 写进度 → `return {...}` 回写新状态：
  - `recall_column.py:21` —— 召回类代表：关键词扩展 → Embedding → Qdrant 检索 → 去重。`recall_value`（ES 全文）同构。
  - `merge_retrieved_info.py:25` —— 最大的节点，看懂它就知道所有召回产物如何聚合成表 / 指标上下文。
  - `generate_sql.py:20` → `validate_sql.py:16` → `correct_sql.py:20` → `run_sql.py:15` —— 完成「生成 → 校验 → 修正 → 执行」的 SQL 闭环。
- 写操作走 HITL 人工审批链路：`classify_sql_type.py`（识别 SELECT/INSERT/UPDATE/DELETE）→ `resolve_insert_primary_key.py`（仅 INSERT，主键冲突自动分配 / 缺主键自动补列，结果以 `pk_note` 并入审批卡片）→ `estimate_impact.py`（预估影响行数，同时用 `SELECT * WHERE...` 抓取执行前快照 `before_data`）→ `wait_for_human_input.py`（`interrupt()` 暂停，审批卡片通过 SSE 下发）→ 确认后 `run_sql.py` 执行并落库审计 / 取消后 `cancel_write.py` 结束。
- 审批续流在 `query_service.py` 的 `resume()`：用户确认 / 取消后，用 `graph.astream(Command(resume=...))` 从断点恢复，前端通过 `POST /api/human-feedback` 触发（`human_feedback_router.py`）。
- 提示词装载统一走 `app/prompt/prompt_loader.py:11` 从 `prompts/*.prompt` 读取。

### 第 4 步：离线构建主线（第二大块）

- `app/scripts/build_meta_knowledge.py:28` 是构建脚本的 controller：初始化客户端 → 组装仓储 → 调 `MetaKnowledgeService.build`。
- `app/services/meta_knowledge_service.py:262` 的 `build()` 按配置分四段：表字段入 MySQL → 字段向量入 Qdrant → 字段取值入 ES → 指标(含依赖关系)入 MySQL + Qdrant。
- 同步哪些表 / 字段 / 指标由 `conf/meta_config.yaml` 声明，结构定义在 `app/conf/meta_config.py:54`。

### 第 5 步：分层基础设施（按需翻阅）

- `app/clients/*_manager.py` —— 统一的 Client Manager 单例范式（参考 `mysql_client_manager.py:58-59` 同时维护 meta / dw 两套连接）。
- `app/repositories/**` —— 数据访问层：`mysql/meta`（读元数据）、`mysql/dw`（读数仓并执行真实 SQL）、`qdrant`（字段 / 指标向量检索）、`es`（取值全文检索）。
- `app/entities/` 是贴近业务语义的数据对象，`app/models/` 是 SQLAlchemy ORM 模型，两者职责不同。
- `app/core/log.py` —— 用 `loguru` 把 `request_id` 注入每条日志，联调排查链路全靠它。

### 第 5.5 步：写操作审计与 Time-Travel 数据回滚（进阶功能）

写操作（增删改）执行成功后，系统会把「执行前快照 + SQL + 影响行数」落库到 meta 库的 `write_audit_log` 表，前端在对应消息左侧展示"回滚"卡片，可一键回到该操作之前（误删 / 误改可自助恢复）：

- 核心原则：**不能用 SQL 逆推还原**（DELETE/UPDATE 会丢失旧值），唯一可靠方案是执行前快照 + 审计日志。
- `estimate_impact.py` 用 `SELECT * WHERE...` 一次拿到影响行数与 `before_data` 快照（快照存入 State，审批通过后随执行链路带到 run_sql）。
- `run_sql.py` 写操作成功后调 `_write_audit_log()` 落库（`session_id` / `seq` / `op_type` / `sql_text` / `before_data` / `row_count` / `status`），并把 `audit_log_id` 随 `result` 事件下发，前端据此把回滚入口挂到对应消息上（`chat_message` 表 `audit_log_id` 列持久化关联）。
- `app/services/rollback_service.py` 实现还原逻辑：DELETE 用快照逐行重插、UPDATE 按主键改回旧值、INSERT 解析最终 SQL 的主键值删除；**LIFO 逆序**连带回滚目标操作之后的所有操作，完成后标记 `rolled_back`（不可重复回滚）。
- 接口：`GET /api/audit-logs?session_id=`（查询会话审计列表）、`POST /api/rollback`（执行回滚，返回被回滚的 log_id 列表）。
- 前端 `RollbackBadge.tsx`：消息左侧竖排卡片（操作类型 / 表名 / 行数 / 回滚按钮），已回滚置灰；`App.tsx` 的 `loadAuditLogs` 维护会话审计列表，回滚后刷新状态。
- 相关仓储：`app/repositories/mysql/meta/write_audit_log_repository.py`（审计读写）；表 DDL 见 `docker/mysql/meta.sql`。

### 第 6 步：前端（最后读，约 1000 行）

- `frontend/src/lib/agentApi.ts:14` —— `streamQuery` / `streamHumanFeedback` 用 `fetch` 读 SSE 流、按 `\n\n` 拆帧、正则取 `data:`、JSON.parse；另有 `listAuditLogs` / `rollback` 回滚接口封装。
- `frontend/src/App.tsx` 和各组件：`Composer`（输入框）、`StepRail`（节点进度展示）、`ResultTable`（结果表格）、`MessageBubble`（历史消息，含写操作审批卡片与回滚卡片挂载）、`HumanApprovalCard`（写操作审批：确认执行 / 取消不执行）、`RollbackBadge`（消息左侧回滚卡片）、`SessionList`（历史会话）。

## 阅读姿势建议

1. **对照 git log 读**：`git log --oneline -10` 查看提交历史，每个 commit 对应一条能力落地的演进。
2. **边读边画依赖图**：为每个模块找出「它 import 谁、被谁 import」：

   ```
   main.py → api/lifespan.py + api/routers/query_router.py + core/context.py
   query_router → api/dependencies.py → services/query_service.py → agent/graph.py → nodes/*
   lifespan → clients/*_manager.py（应用级单例初始化）
   所有模块 → conf/app_config.py（配置） + core/log.py（日志）
   ```

3. **找扩展点**：想加一种召回，在 `graph.py` 注册新节点并加边，再仿 `recall_column.py` 写实现；想调行为，`conf/*.yaml`、`prompts/*.prompt`、`.env` 就是可调旋钮。
4. **用调试脚本跑链路**：`app/agent/graph.py:96` 自带的 `test()` 可以直接本地串起三路召回，配合日志观察 State 变化。

**最快路径**：先跑一遍构建脚本 `uv run python -m app.scripts.build_meta_knowledge -c conf/meta_config.yaml`，再 `uv run fastapi dev main.py` 发起一次 `/api/query` 请求，观察 SSE 里节点的执行顺序；然后顺着 graph.py → query_service.py → 几个节点把在线链路读通，最后再补离线构建和前端。