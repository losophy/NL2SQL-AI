# 测试提示词（增删查改）

> 用途：复制下面的提示词到 NL2SQL Agent 对话框，逐项验证"增/删/查/改"能力。
> 环境：dw 数仓现有 5 张表（dim_server / dim_player / dim_item / dim_date / fact_recharge）。
> 当前数据：dim_server 8 行；dim_player 22 行（含残留测试行 C021 蝙蝠侠、C022 无名氏）；dim_item 15 行（皮肤6/礼包4/消耗品3/通行证2）；dim_date 90 行；fact_recharge 125 行。

## 能力说明（重要）
- **查（SELECT）**：已实现，直接返回结果。
- **增（INSERT）/ 改（UPDATE）/ 删（DELETE）**：已实现，且带**人工审批（HITL）**——生成 SQL 后需在前端点"通过"才真正执行；点"取消不执行"则收回审批卡片、不改动数据。执行成功后结果区展示**受影响表的数据**（不是"影响行数"）。
- **主键自动处理**：INSERT 若主键冲突会自动替换为不冲突值，若漏写主键列会自动补列。
- **建表 / 删表（CREATE / DROP TABLE）**：Agent **不自动执行**，仅生成可执行的 DDL 语句并附手动步骤提示。
- ⚠️ 增/改/删用例点"通过"后会**真实修改 dw**，建议先用"取消不执行"走一遍流程确认 SQL，再真正执行。

---

## 一、查（SELECT）

### 用例1 · 单表条件查询
输入：
> 查询 dim_player 里所有男性玩家

预期：返回 dim_player 中 gender='男' 的行。

### 用例2 · 聚合统计（分组）
输入：
> 各服务器的充值总额分别是多少？

预期：fact_recharge JOIN dim_server，按服务器分组 SUM 金额，返回各服务器总额。

### 用例3 · 多表关联
输入：
> 查询 VIP3 及以上玩家在 2025 年 1 月的充值总额

预期：dim_player JOIN fact_recharge，过滤 vip_level 与日期后聚合。

### 用例4 · 表元数据（快捷路径）
输入：
> 数据库里有哪些表？

预期：不走完整流程，直接列出 5 张表并展示各表数据（表数 ≤ 10 显示全量）。

---

## 二、增（INSERT，人工审批）

### 用例5 · 维度表单条新增（主键自动分配）
输入：
> 在 dim_player 表里新增一个玩家，昵称叫"测试侠"，性别男，VIP等级 VIP3

预期：生成 INSERT → 弹出审批卡片（若 LLM 编造的主键与已有冲突会自动替换为新主键，如 C011→C021）→ 点"通过"后落库，结果区展示 dim_player 表。

### 用例6 · 事实表带外键新增
输入：
> 给 fact_recharge 插入一条充值记录：玩家 C001，服务器 S001，道具 I01，日期 20250101，数量 1，金额 30

预期：生成带外键的 INSERT → 审批后落库。

### 用例7 · 多行批量新增（自动补主键列）
输入：
> 给 dim_item 同时加两个道具："测试皮肤A"（品类 皮肤）和"测试礼包B"（品类 礼包）

预期：多条 INSERT 合并为一个审批；若某行漏写 item_id 主键列，会自动补列并分配新主键。

### 用例8 · 边界·缺主键（自动补列）
输入：
> 给 dim_player 新增一个玩家，叫"无名氏"，VIP0

预期：LLM 漏写 player_id 时，系统自动补列并分配新主键，审批后落库（不再报主键缺失）。

### 用例9 · 边界·主键冲突（自动替换）
输入：
> 新增玩家 C011，昵称"新苏苏"，性别女

预期：C011 已存在 → 预检自动替换为不冲突主键（如 C023）→ 审批卡 SQL 显示新主键 → 点"通过"落库。

---

## 三、改（UPDATE，人工审批）

### 用例10 · 按条件改单列
输入：
> 把玩家 C021 的 VIP 等级改成 VIP5

预期：生成 `UPDATE dim_player SET vip_level='VIP5' WHERE player_id='C021'` → 审批后执行，结果区展示 dim_player 表。

### 用例11 · 多列修改
输入：
> 把所有 VIP3 玩家的性别改成女，VIP 等级改成 VIP4

预期：生成带 WHERE（vip_level='VIP3'）的 UPDATE，多列 SET → 审批后执行。

### 用例12 · 边界·缺 WHERE（拦截）
输入：
> 把所有玩家的 VIP 等级都改成 VIP15

预期：缺 WHERE，应被**拦截**并要求限定范围（不生成无 WHERE 的全表 UPDATE）。

---

## 四、删（DELETE，人工审批）

### 用例13 · 按主键删单条
输入：
> 删除 dim_player 里玩家ID为 C021 的玩家

预期：生成 `DELETE FROM dim_player WHERE player_id='C021'` → 审批后删除该行，结果区展示 dim_player 表（剩 21 行）。
验证追问：
> C021 这个玩家还在吗？

### 用例14 · 按非主键条件删多行
输入：
> 删除 dim_item 中品类为"皮肤"的所有道具

预期：生成 `DELETE FROM dim_item WHERE item_category='皮肤'` → 审批后删除 6 行，结果区展示 dim_item 表。

### 用例15 · 按范围条件删
输入：
> 删除 fact_recharge 里日期在 20250115 之前的所有充值记录

预期：生成带 `date_id < 20250115` 的 DELETE → 审批后删除该范围行，结果区展示 fact_recharge 表。

### 用例16 · 边界·缺 WHERE（拦截）
输入：
> 把 dim_player 里所有玩家都删除

预期：缺 WHERE，应被**拦截**并要求限定范围（不生成无 WHERE 的全表 DELETE）。

### 用例17 · 边界·删不存在的数据
输入：
> 删除 dim_player 里玩家ID为 Z999 的玩家

预期：生成 DELETE 但无匹配 → 审批通过后影响 0 行，结果区展示 dim_player 表（行数不变），提示"未删除任何行"。

---

## 五、建表 / 删表（仅生成 DDL，不自动执行）

### 用例18 · 建表（生成可执行 DDL）
输入：
> 在数仓新建一张玩家登录流水表 fact_login，字段：登录ID（主键）、玩家ID、服务器ID、日期、登录次数、在线分钟数

预期：
- 执行流程 3 步（识别意图 → 生成 DDL → 返回提示）；
- "执行的SQL语句"给出可复制的 `CREATE TABLE fact_login(...)`；
- 结果区附 4 步手动建表提示；
- **dw 实际未建表**（Agent 不执行）。

### 用例19 · 删表（生成 DROP）
输入：
> 删除 dim_player 表

预期：
- "执行的SQL语句"给出 `DROP TABLE IF EXISTS dim_player;`；
- **dw 实际未删表**（Agent 不执行）；
- 提示删表属高危，还需级联清理 meta / Qdrant / ES。

---

## 附：建表/删表的手动同步流程（如你真要执行 DDL）
1. 复制 Agent 给出的 `CREATE TABLE` / `DROP TABLE` SQL，在 dw 数仓执行；建表后可用 `INSERT` 灌示例数据；
2. 编辑 `conf/meta_config.yaml`，在 `tables:` 下**追加/删除**该表定义（列名/角色/别名/描述）；
3. 重跑元数据构建（重跑前**先清空 meta 4 张表**，否则 `column_info` 主键冲突）：
   `uv run python -m app.scripts.build_meta_knowledge -c conf/meta_config.yaml`
4. 验证：问数能命中/不再命中该表即同步成功。
